/**
 * Daily Log tab - entry point for the Daily Log tab navigation
 * Renders the DailyLogScreen with date selection and daily totals
 */

import { DailyLogScreen } from '@/screens/DailyLogScreen';

export default function DailyLogIndex() {
  return <DailyLogScreen />;
}
