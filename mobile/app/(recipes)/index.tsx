/**
 * Recipes list tab - entry point for the Recipes tab navigation
 * Renders the RecipeListScreen with search and filtering
 */

import { RecipeListScreen } from '@/screens/RecipeListScreen';

export default function RecipesIndex() {
  return <RecipeListScreen />;
}
