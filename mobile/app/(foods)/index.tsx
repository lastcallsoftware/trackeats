/**
 * Foods list tab - entry point for the Foods tab navigation
 * Renders the FoodListScreen with search and filtering
 */

import { FoodListScreen } from '@/screens/FoodListScreen';

export default function FoodsIndex() {
  return <FoodListScreen />;
}
